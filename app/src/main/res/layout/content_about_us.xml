<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.sweedelight.www.sweedelight.AboutUs"
    tools:showIn="@layout/activity_about_us">

    <include
        android:id="@+id/toolbar"
        layout="@layout/tool_bar" />

    <WebView
        android:id="@+id/company_info"
        android:layout_below="@id/toolbar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
       />

    <TextView android:id="@+id/copyright_info"
        android:layout_below="@id/company_info"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:gravity="center"
        android:text="@string/copyright_content"
        />

</RelativeLayout>
